{
  "name": "@placepin/about",
  "version": "0.1.0",
  "private": true,
  "description": "about.placepin.org — what the protocol is, and the specification in full.",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-spec.mjs",
    "dev": "npm run sync && vitepress dev docs",
    "build": "npm run sync && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@placepin/vectors": "git+https://github.com/placepin-org/placepin-spec.git#main",
    "vitepress": "^1.6.4",
    "vue": "^3.5.13",
    "wrangler": "^4.125.0"
  }
}
